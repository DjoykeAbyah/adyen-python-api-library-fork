# name: Close Stale Issues
# on:
#   schedule:
#     - cron: '30 8 * * *'

# jobs:
#   stale:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/stale@v9
#         with:
#           repo-token: ${{ secrets.GITHUB_TOKEN }}
#           stale-issue-message: 'This issue has been automatically marked as stale due to inactivity and will be closed in 7 days if no further activity occurs.'
#           close-issue-message: 'This issue was closed due to inactivity. Please reopen if you still encounter this problem or have more information to add.'
#           days-before-stale: 0
#           days-before-close: -1
#           stale-issue-label: 'stale'
#           exempt-issue-labels: 'do not stale, needs response'
#           exempt-pr-labels: 'do not stale'

name: Close Stale Issues
on:
  schedule:
    - cron: '30 8 * * *'

jobs:
  stale:
    runs-on: ubuntu-latest
    steps:
      - name: Install GitHub CLI
        run: |
          curl -sSL https://cli.github.com/install.sh | sh

      - name: List Issues
        run: |
          gh issue list --limit 100 --json number > issues.json

      - name: Prepare Issue Numbers
        run: |
          jq -r '.[].number' issues.json > issue_numbers.txt

      - name: Run Stale Action
        uses: actions/stale@v9
        with:
          repo-token: ${{ secrets.GITHUB_TOKEN }}
          days-before-stale: -1
          days-before-close: -1
          stale-issue-message: 'This issue has been automatically marked as stale due to inactivity and will be closed in 7 days if no further activity occurs.'
          close-issue-message: 'This issue was closed due to inactivity. Please reopen if you still encounter this problem or have more information to add.'
          days-before-issue-stale: 0
          days-before-issue-closed: 1
          stale-issue-label: 'stale'
          exempt-issue-labels: 'do not stale, needs response'
          issue-only: true
          issue-list-path: issue_numbers.txt

